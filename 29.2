process creation 

  system()
  fork()
  wait()
  waitpid()
  clone()

  system() is  library function
  fork,wait,waitpid,exec,clone they are system call

  man 2     system calls
   man fork system call

   fork 
      create new process by duplicating the call process 
      creating child process
